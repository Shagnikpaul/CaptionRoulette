# Day 1 Completed Tasks

- Generated the Spring Boot project with the required dependencies.
- Decided on the repository structure and initialized the project.
- Verified the Spring Boot application starts successfully.
- Created the backend package structure.
- Configured `application.yml` using environment variables.
- Learned the purpose of `@SpringBootApplication`, auto-configuration, and component scanning.
- Created a Neon PostgreSQL project and obtained the connection string.
- Configured the backend to connect to Neon.
- Set up Flyway for database migrations.
- Created the initial Flyway migration for all required tables.
- Implemented the required primary keys, foreign keys, constraints, and defaults.
- Verified Flyway runs successfully on application startup.
- Created the React (Vite + TypeScript) frontend project.
- Configured Tailwind CSS.
- Configured React Router.
- Configured TanStack Query.
- Created the initial frontend folder structure.
- Created a basic application layout with routing.
- Created the required cloud service accounts (Neon, Render, Netlify, Cloudflare, Upstash, and optional Groq).
- Initialized Git and configured `.gitignore`.
- Pushed the initial project to GitHub.


